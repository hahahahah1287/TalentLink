# TalentLink AI 后端依赖
#
# 说明：依赖按「实际被代码 import」整理，去掉未使用的包以减少下载体积。
# 运行链路：main.py → workflow_service.py → LangGraph workflows + skills。

# === Web 框架 ===
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# === LangChain / LangGraph 核心 ===
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-experimental>=0.0.60     # ChatLlamaCpp
langgraph>=0.2.0                    # StateGraph 工作流编排

# === 本地 LLM (llama-cpp-python) ===
llama-cpp-python>=0.2.23

# === 环境变量 / LLM-as-Judge（可选，对应 RAGAS_TEST_PLAN.md §17.3） ===
python-dotenv>=1.0.0          # .env 加载（防 key 泄露到 git）
google-genai>=1.0.0           # Gemini 作为 judge（用 .env 的 GEMINI_API_KEY）

# === Embedding & Reranker（仅用 transformers + torch，不依赖 sentence-transformers） ===
transformers>=4.30.0
torch>=2.0.0

# === 向量数据库 / 数值计算 ===
faiss-cpu>=1.7.4                    # 有 GPU 可换 faiss-gpu
numpy>=1.24.0                       # 语义缓存余弦相似度 + 案例检索向量运算

# === 检索 / 分词 ===
rank_bm25>=0.2.2                    # BM25 关键词检索
jieba>=0.42.1                      # 中文分词（评估指标 + 案例要素抽取）

# === 数据库 ===
pymysql>=1.1.0
DBUtils>=3.0.0
redis>=4.5.0

# === 搜索 ===
duckduckgo-search>=3.9.0
